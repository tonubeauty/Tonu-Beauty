{
  "entities": {
    "orders": {
      "title": "Orders",
      "description": "Customer bookings and appointments",
      "properties": {
        "orderId": { "type": "string" },
        "customerName": { "type": "string" },
        "phone": { "type": "string" },
        "address": { "type": "string" },
        "totalAmount": { "type": "number" },
        "status": { "type": "string" },
        "createdAt": { "type": "string" }
      }
    },
    "products": {
      "title": "Products and Services",
      "description": "Beauty parlour services and laser treatment catalog",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "titleBn": { "type": "string" },
        "price": { "type": "number" },
        "category": { "type": "string" }
      }
    },
    "settings": {
      "title": "System and Parlour Settings",
      "description": "Global configuration including QR code view mode (text memo vs URL) and parlour details",
      "properties": {
        "id": { "type": "string" },
        "qrTextMemoEnabled": { "type": "boolean" },
        "updatedAt": { "type": "string" }
      }
    },
    "appointments": {
      "title": "Appointments",
      "description": "Customer parlour appointments, tracking received status, and follow-up communication",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "phone": { "type": "string" },
        "address": { "type": "string" },
        "service": { "type": "string" },
        "date": { "type": "string" },
        "dateDisplay": { "type": "string" },
        "status": { "type": "string" },
        "createdAt": { "type": "string" }
      }
    },
    "appointment_services": {
      "title": "Appointment Services Catalog",
      "description": "Saved parlour services remembered as categories for one-click re-selection",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "count": { "type": "number" }
      }
    }
  }
}
